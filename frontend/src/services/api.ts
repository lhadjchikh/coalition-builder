// Re-export the shared API client for backwards compatibility
export { frontendApiClient as default } from './frontend-api-client';

// Re-export ContentBlock type from shared types
export type { ContentBlock } from '@shared/types';
