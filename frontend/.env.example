# API Configuration
# For development, API calls are proxied through Next.js rewrites
# For production, leave empty to use relative paths (handled by CDN/proxy)
NEXT_PUBLIC_API_URL=

# Internal API URL for server-side rendering (SSR)
# This is used by the Next.js server to fetch data during SSR
API_URL=http://localhost:8000

# Image Sources Configuration
# CloudFront domain for serving images from CDN
# Example: d123456789.cloudfront.net or cdn.example.com
CLOUDFRONT_DOMAIN=

# S3 bucket name for Next.js image optimization of S3-hosted media
# Example: coalition-static-assets-a4853294
AWS_STORAGE_BUCKET_NAME=

# Backend domain if different from CloudFront (optional)
# Used when backend serves images directly without CDN
BACKEND_DOMAIN=

# Google Analytics
NEXT_PUBLIC_GA_TRACKING_ID=

# Feature Flags (optional)
NEXT_PUBLIC_ENABLE_ANALYTICS=true